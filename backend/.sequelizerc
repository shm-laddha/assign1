const path = require('path');
require('dotenv').config();

console.log(process.env.POSTGRES_URL);

module.exports = {
  config: path.resolve('src', 'database', 'sql', 'config', 'config.json'),
  'seeders-path': path.resolve('src', 'database', 'sql', 'seeders'),
  'migrations-path': path.resolve('src', 'database', 'sql', 'migrations'),
  'models-path': path.resolve('src', 'database', 'sql', 'models')
};
